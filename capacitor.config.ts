import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.explorando.app',
  appName: 'explorando',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
